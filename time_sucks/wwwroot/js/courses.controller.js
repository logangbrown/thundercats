angular.module('time').controller('CoursesCtrl', function ($scope, $http, $routeParams, $location, userService) {
    //Check that a user is logged in, redirect to login if not
    //if (jQuery.isEmptyObject(userService.get())) { $location.path('/login'); } else {
        
    //}
});